import {api} from "../Configs.js"

export async function getClientsComplaints(){
/*    try {
        const responseData = await api.get(`/regRequests/active`);
        return responseData;
    } catch (err) {
        console.log(err.message);
        return err.message
    }*/
  }